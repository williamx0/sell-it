spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Sell-it-users
    username: postgres
    password: 080277
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect